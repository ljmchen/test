# 修改记录

本文件记录对本项目（DexVLG 双手灵巧抓取复现）的重要改动，按时间倒序排列。

---

## 2026-06-30（二）— EMA + 向量化验证 + CFG + 手部交互 + 隐式 Affordance

本次一共实现 5 项改动，**全部通过配置开关控制，默认关闭**，与基线行为完全一致。

### P0: Exponential Moving Average（EMA）

- **文件**：`train.py`
- **新增**：`_EMA` 类（deepcopy 模型、lerp\_ 更新参数、copy\_ 缓冲区）。
- **训练**：每次 optimizer step 后调用 `ema.update(model)`，对所有参数做
  `shadow = decay * shadow + (1-decay) * model`。
- **验证**：用 EMA shadow 模型（而非训练模型）做采样验证，使指标更平滑、选出更好的 checkpoint。
- **保存/恢复**：checkpoint 增加 `ema_state_dict` 字段；resume 时自动恢复。不影响
  `model_state_dict`（始终保存训练模型权重），与 test.py / infer\_dataset.py 兼容。
- **配置**：`training.ema: {enabled: false, decay: 0.9999}`

### P3: 向量化组内最小匹配（val\_group\_min 加速）

- **文件**：`train.py::validate`
- **改动**：将原来 `for b in range(bsz)` 的逐样本 Python 循环改为**批量向量化**：
  - 收集当前 batch 所有样本的候选，padding 到 max\_K，构建 (B, K, 31) 张量 + bool mask。
  - 用 broadcasting 一次调用 `pose_component_errors`（pred (B,1,31) vs cands (B,K,31)）。
  - `masked_fill_(inf)` 无效位 → `argmin` 找最佳候选 → `gather` 取对应误差。
- **效果**：消除 O(B) 次 Python→CUDA 调用，大 batch 下显著加速（尤其 K 较大时）。
  数值结果与原逐样本循环完全一致。

### A1: Classifier-Free Guidance（CFG）

- **文件**：`models/dexvlg.py`、`configs/default.yaml`
- **原理**：训练时以概率 `drop_prob` 将 fused condition tokens 替换为可学习的 `null_cond`
  （一个 (1,1,bert\_dim) 的 Parameter，广播到整个 condition 序列）。推理时做**双重前向**：
  `v = v_uncond + guidance_scale * (v_cond - v_uncond)`，放大条件信号。
- **兼容性**：`drop_prob=0` 时不创建 `null_cond` Parameter（state\_dict 与基线完全一致）。
  `guidance_scale=1.0` 时推理走单前向（无额外计算）。
- **配置**：`model.cfg: {drop_prob: 0.0, guidance_scale: 1.0}`
- **典型用法**：`drop_prob: 0.1, guidance_scale: 2.0`（需重训）。

### A4: 手部交互建模（Hand Interaction）

- **文件**：`models/flow_matching.py`、`models/dexvlg.py`、`configs/default.yaml`
- **Per-hand type embedding**：`nn.Embedding(num_queries, dim)`，在 flow transformer
  的 input\_proj 后加到每个手的 query token 上（左手 id=0、右手 id=1），让模型区分左右手。
- **可选 cross-hand attention**：在所有 FlowMatchingBlock 之后、final\_norm 之前，
  加一层 `nn.MultiheadAttention`（self-attention over hand queries），让两只手的
  query 互相交流。仅在 `cross_hand_attn: true` 时创建。
- **兼容性**：`enabled: false` 时不创建任何新参数（state\_dict 与基线一致）。
- **配置**：`model.hand_interaction: {enabled: false, cross_hand_attn: false}`

### Affordance Plan B: 隐式 Affordance 注意力（已实现）

- **文件**：`models/dexvlg.py`、`configs/default.yaml`
- **原理**：在 fusion transformer 输出后，用一个小 MLP head (`bert_dim → hidden → 1`)
  对每个 PC token 打分 → softmax 得到 per-token 权重 → 加权求和生成一个**任务相关摘要
  token** → 拼接到 fused token 序列末尾，作为额外的 condition 给 flow transformer 做
  cross-attention。
- **无额外损失**：affordance 分数的梯度完全来自 flow-matching loss（"哪些点对生成正确抓取
  最重要"由端到端训练自动学习）。
- **配置**：`model.affordance: {enabled: false, hidden_dim: 192}`

### Affordance Plan C: 显式 Affordance（仅记录，未实现）

Plan C 的设计思路，保留供后续实现参考：

1. **独立 affordance 标注**：每个点云点标注一个 0/1 或连续 [0,1] 的 affordance 分数，
   表示该点对当前任务（由 language 指定）的相关度。标注来源可以是：
   - 接触图（contact map）：与手部网格接触的点 → 1，其余 → 0；
   - 抓取成功率热图：仿真中不同区域的抓取成功率；
   - 人工标注 / LLM 辅助标注。
2. **显式 affordance loss**：在 fusion 输出后用 MLP 预测每点 affordance，加一个 BCE/MSE
   loss（权重可调）与 flow-matching loss 联合训练。
3. **优点**：直接监督，affordance 分布可解释、可可视化；可用于引导探索（高 affordance
   区域优先采样）。
4. **缺点**：需要额外标注数据（接触图或 affordance GT）；多一个超参（loss 权重比）；
   标注质量直接影响效果。
5. **与 Plan B 的关系**：Plan B 是 Plan C 的"弱监督近似"——不需要显式标注，端到端学习
   隐式 affordance。如果 Plan B 效果不足，可升级到 Plan C 以获得更强的空间引导。

---

## 2026-06-30 — 修复训练 NaN（fp16→bf16）+ non-finite 跳步

- 现象：ModernBERT + fp16 训练，前 ~44 epoch 正常(loss 1.7→0.4)，之后**突然 NaN 且永久不恢复**
  （连瞬时 loss 都 nan）。
- 根因：fp16 下 ModernBERT/PointNet2 的激活会**溢出成 inf**，污染 forward 里更新的
  **BatchNorm running stats**（以及产生 nan loss）；GradScaler 只能跳过 optimizer step、
  **救不了这些 forward-buffer 污染**，所以一旦中招就永久 nan。
- 修复（`train.py` + config）：
  - 新增 `get_amp_settings`，支持 `training.precision: bf16 | fp16 | fp32`（旧 `fp16` bool 作回退）。
    **bf16 时 autocast 用 bfloat16、关闭 GradScaler**（H100 原生、范围等同 fp32、不溢出）。
  - train_one_epoch / validate 的 autocast 改用解析出的 dtype；启动日志打印 precision。
  - 加 **non-finite loss 跳步保险**：loss 非有限则 zero_grad + 跳过该 step（不踩进 nan 权重）。
  - config 默认改 `precision: bf16`。
- 验证：bf16 端到端跑通无 nan；fp16/fp32/legacy 各模式解析正确。

---

## 2026-06-29（四）— 验证组内最小损失匹配（val_group_min）

- 现象：原验证是**一对一**——每条记录采样1个抓取只和**自己的GT**比，多模态下"合法但不同"的抓取被
  惩罚，导致验证指标和成功率脱钩（best2 选到成功率最差的 epoch）。
- 新增 `training.val_group_min`（默认 false，保留原行为）。开启后：每个预测和它所在
  **(obj_id, pose_id, guidance)** 组里**所有GT抓取**比，取**最接近的那个**（按 trans+rot+joint 联合
  最小的双手候选）计误差。与 epoch_sampling 无关——候选始终取自**完整验证集**。
- 实现：
  - `data/dataset.py`：item/collate 携带 `group_id`（兼容 guidence 拼写）；新增 `group_id_of`。
  - `train.py`：`build_group_candidates`（一次性构建 group→全部GT候选，反归一化到真实单位、缓存跨epoch）、
    `pose_component_errors`（逐候选误差，支持 mse/物理度量），`validate` 增 `group_candidates` 分支。
- 验证：候选集构建正确、组内匹配跑通、`group-min score ≤ one-to-one`、与 epoch_sampling 共存。
- 成本：首次验证时遍历整个验证集构建候选（mesh bbox 缓存，远端秒级），之后缓存复用。

---

## 2026-06-29（三）— 语言加载严格化 + 分组重采样训练链路

### 1. 语言模型加载失败改为报错（`models/dexvlg.py`）
- 配置的是**本地路径**（解析到存在的目录）却加载失败 → `RuntimeError`，不再静默回退随机权重。
- 名字**看着像本地路径**（`pretrained_models/`、`./`、`/` 等开头）但目录不存在 → `FileNotFoundError`
  （挡住 typo 路径被当成 HF id 静默回退）。仅非本地 HF id 离线失败才回退随机（CI 用）。
- 排查其它静默回退：`data/dataset.py` 的 `point_cloud_source: auto` 会在彩色 PLY 缺失/点数不符时
  静默回退 mesh 采样——这是 auto 模式的设计行为（当前 config 用 mesh_sample 不触发）；其余 except
  均为可选依赖导入或格式化兜底，无吞错风险。

### 2. 新增分组重采样训练链路（`data/samplers.py` + `train.py` + config）
- 新增 `GroupedEpochSampler`：按 `group_keys`(默认 obj_id+pose_id+guidance，兼容 guidence 拼写) 分组，
  **每个 epoch 从每组随机取 `samples_per_group` 个样本**；DDP 感知（各 rank 同种子构同表后均匀分片）。
- `train.py`：`training.epoch_sampling.enabled=true` 时 train 用该采样器（每 epoch 重采样），
  val 用固定子集（epoch0，`val_samples_per_group` 个/组，保证指标可比）；`enabled=false` 走原全量方法。
- config 新增 `training.epoch_sampling`。
- 验证：分组封顶、跨 epoch 重采样、DDP 无重叠分片、guidence 兼容均通过；tiny 端到端训练正常
  （6条1组→每epoch取2→2步）。

---

## 2026-06-29 — 去重多样本测试推理（inference2）

- `infer_dataset.py` 新增两个参数：
  - `--dedup-by KEY1,KEY2,...`：按这些字段对输入记录**去重**（只保留每个组合第一条），
    避免对相同点云+指令重复推理。`guidance` 自动兼容 `guidence` 拼写。空=不去重(原行为)。
  - `--samples-per-combo N`：每个(去重后)组合**采样 N 份**。flow matching 从随机噪声采样，
    同一输入复制 N 次即得 N 个不同抓取。每份输出一条记录，带 `candidate_idx` 0..N-1。
- 新增 `scripts/inference2.sh`：默认 `--dedup-by obj_id,pose_id,guidance`，其余参数透传
  （含 `--samples-per-combo`）。沿用 inference.sh 的远端 env 激活。
- 下游 `prepare_bench_data.py` 按 `candidate_idx` 把 N 份采样分成同一 sample 的 N 个 variant
  （`0.npy/1.npy/.../N-1.npy`），bench 即可评测每组合的多候选（多样性/成功率）。
- 验证：6 条样本→去重 1 组合→每组 3 样本→3 条记录(candidate 0/1/2)，样本间抓取确实不同；
  prepare_bench 产出 3 个 variant npy。

---

## 2026-06-28（七）— 配置备份 + 验证分量损失可切 MSE

### 1. 训练启动时备份配置（`train.py`）
- rank0 在训练开始时把**生效的完整 config** `yaml.safe_dump` 到 `{output_dir}/config.yaml`，
  便于复现与对照 checkpoint。日志里打印备份路径。

### 2. 验证分量损失可逐项切换为 MSE（`train.py` + config）
- 新增 `training.val_loss_mse: {translation, rotation, joint}` 三个开关（默认全 false）。
- 某分量为 true → 该分量用 **MSE**（逐元素均方）；false → 维持原物理度量
  （平移 L2 距离 / 旋转测地角 / 关节 L1）。可混用。
- 度量在**反归一化后的真实单位**空间计算（与现有指标同坐标系）。
- 顺带：验证日志去掉硬编码的 "m"/"rad" 单位后缀（MSE 模式下不成立）。
- 验证：tiny 训练确认 config.yaml 正确备份（含 val_loss_mse），全开 MSE 后
  trans/joint 数值明显变小（MSE 特性）。

---

## 2026-06-28（六）— 数据加载提速 + 归一化优化 + ModernBERT 可选骨干

### 1. DataLoader 提速（`train.py`）
- `num_workers>0` 时自动加 `persistent_workers=True` + `prefetch_factor=4`：worker 跨 epoch
  存活，点云缓存不再每个 epoch 冷掉，I/O 与 GPU 步重叠。config 加了同名开关。

### 2. 归一化优化（`data/pose_normalizer.py`）
- `normalize_pose`/`denormalize_pose` 去掉对 6D 旋转的冗余重正交化（训练目标已正交、模型
  输出在下游 `rotation_6d_to_matrix` 会再正交化）。`normalize_pose` 70µs→32µs。
- 实测：归一化对训练速度影响极小（每样本 +0.07~0.2ms），**不是** 5→2 it/s 的原因；
  真正瓶颈是点云加载 I/O（见第 1 点缓解）。

### 3. ModernBERT 可选语言骨干（`models/dexvlg.py` + config）
- 新增 `model.language_backbone: bert(默认) | modernbert`。modernbert 走 `AutoModel/AutoTokenizer`
  加载（接口与 BERT 一致：input_ids/attention_mask → last_hidden_state），离线/缺权重时回退
  随机 BERT。`freeze_bert_embeddings` 对无 `.embeddings` 的骨干安全跳过。
- ModernBERT-base hidden_size=768，与 `bert_dim` 对齐，近乎 drop-in。
- 权重下载到 `pretrained_models/ModernBERT-base`。启用：
  `language_backbone: modernbert` + `bert_model: pretrained_models/ModernBERT-base`。
- 已验证：构建 + forward(loss 2.25) + backward + 语言参数有梯度。
- 注意：换骨干属改模型，旧 checkpoint 失效需重训。

---

## 2026-06-28（五）— 评测链路：推理 + pre/squ+格式转换 + bench 脚本

对齐 dexvlm 的「推理 → 获取 pre/squ → 转 eval3 格式 → 跑 bench」命令流，给 test 写了一套：

1. **项目内推理**：`infer_dataset.py` + `scripts/inference.sh`。在某个 split 上批量采样，
   反归一化、rot6d→axis_angle 得到每手 28 维 `[trans(3), axis_angle(3), joints(22)]`，
   `center_on_object` 时还原回世界坐标，输出 `predictions_test.json`（保留原记录字段 +
   `pred_left`/`pred_right` + 元数据）。对应 dexvlm 的 `project/training/inference.py`。
   - 为此给 `DexGraspDataset` 加了 `get_inference_item`（返回点云 + 世界还原偏移 + 原记录）。
2. **二合一 pre/squ + 格式转换**：`prepare_bench_data.py` + `scripts/prepare_bench.sh`。
   合并 dexvlm 的 `get_pre_squ_dexvlm.py`（算 pregrasp/squeeze）与 `prepare_lgbidex_eval3.py`
   （28 维 GYS pose → 29 维 bench qpos：axis_angle→wxyz 四元数、PALM_OFFSET、Shadow 关节符号
   修正；按 obj/pose/act/grasp 布局存 `.npy`）。姿态数学逐字复刻，产物与 dexvlm 字节级兼容。
   直接写到 `<output_root>/<exp_name>_<hand>/graspdata`。
3. **bench 评测脚本**：`temp/dgbench-pami/script/slaitest_bench_all.sh`（dexvlm_bench_all.sh 的
   平行版），跑 `src/main.py task=eval3/stat3`，`--exp-name` 指定实验，HAND_NAME 默认 shadow_left。

验证：本机用 tiny checkpoint 跑通步骤 1、2——predictions 每手 28 维 world、graspdata `.npy`
含 `obj_path/obj_pose(7)/obj_scale/left·right_{pre,grasp,squeeze}_qpos(29)`、目录布局与 bench 一致。
命令流见 `运行命令.md`。

---

## 2026-06-28（四）— 验证回退 epoch 触发 + 加入数据集归一化

### 1. 验证回退为 epoch 触发（撤销上一轮的按步触发）
- 在 `main` 的 epoch 循环里按 `(epoch + 1) % val_interval == 0` 触发验证 + best-K，
  **避免 epoch0 触发**（val_interval=5 时在第 5/10 个 epoch 验证）；末尾补一次最终验证，
  若最后一个 epoch 已验证则跳过（不重复）。`train_one_epoch` 内的按步验证逻辑已移除。
- config：`val_interval` 1000(step) → **1(epoch)**。`val_max_batches`/`keep_best_k` 保留。

### 2. 加入数据集姿态归一化（移植 dexvlm minmax11，逐手）
- 新增 `data/pose_normalizer.py`：`LgbidexPoseNormalizer`（minmax11，6D 旋转，
  left/right 各一套 factor 表，自 dexvlm 逐字移植）。平移 + 22 关节 min-max 归一到
  [-1,1]；6D 旋转仅重正交化（测地意义不变）。
- `data/dataset.py`：新增 `normalization` 参数，`_build_hand_pose` 对每只手按对应
  left/right normalizer 归一化目标 pose。
- `train.py`：dataset 传入 `normalization`；`main` 构建 left/right denormalizer 放进
  验证上下文；`validate` 在算误差前把预测与 GT **反归一化回真实单位**（米/弧度），
  使三项损失与选 ckpt 的 score 可解释。
- `test.py` / `inference.py`：评估/推理同样在采样后反归一化，指标与导出 pose 为真实单位。
- config：`data.normalization: {enabled: true, mode: minmax11}`。
- 验证（GPU 微型训练，归一化开启）：训练在归一化空间进行，验证反归一化后
  trans≈0.46m、joint≈0.56rad（未归一化版本 trans≈1.8m），数量级正确；epoch 触发、
  best-K 保留均正常。
- 注意：归一化会**改变 checkpoint 语义**（模型学的是归一化 pose）。开/关归一化的
  checkpoint 不可混用；切换后需重训。

---

## 2026-06-28（三）— 文本日志 + 验证改为按步触发 + 验证可裁剪

- **文本日志**：新增 `setup_logging`，rank0 把训练/验证关键信息同时写到控制台与
  `{output_dir}/{log_dir}/train.log`（含启动配置、参数量、每 `log_interval` 步的
  train_loss/lr、每次验证的三项指标与 checkpoint 保留情况、收尾汇总）。tqdm 进度条仍只在
  控制台。
- **验证改为按步触发**（修复"5 太久 / epoch0 就验证"）：原 `val_interval` 按 epoch 用，
  `epoch % 5 == 0` 在 epoch0 即触发、且大数据集下一个 epoch 极长。现改为**每
  `val_interval` 个 optimizer step** 触发（在 `train_one_epoch` 内），首次在 step=val_interval、
  不会在 step0/epoch0 触发。末尾再补一次最终验证（若最后一步刚验证过则跳过，避免重复）。
- **`val_max_batches`**：新增配置，限制每次验证只跑前 N 个 batch（0=整个验证集），
  便于"频繁但低成本"地监控。
- **config**：`val_interval` 5(epoch) → 1000(step)；新增 `val_max_batches: 20`；
  `log_interval` 3000 → 100。
- 验证（GPU 微型训练）：train.log 正确生成；验证在 step 2/4/6 触发、无重复；
  keep_best_k 正确保留最优、淘汰最差。
- 说明：**数据集目前未做姿态归一化**（raw pose 进 flow-matching）。dexvlm 用
  `LGBidexPoseNormalizer`(minmax11) 把平移/关节归一到 [-1,1]，与 flow 的 N(0,1) 先验更匹配；
  test 这边为保持模型/推理一致暂未加。若要加需同步 dataset 归一化 + validate/inference 反归一化
  （会改变 checkpoint 语义），按需另行实现。

---

## 2026-06-28（二）— 采样式验证 + best-K 检查点 + 分布式/xyz-only 修复

### 1. 修复分布式启动崩溃（`train.py::setup_distributed`）
- 原逻辑只判断 `RANK in os.environ` 就进分布式，shell 里残留 `RANK` 但无 `LOCAL_RANK`
  时崩 `KeyError: 'LOCAL_RANK'`。
- 改为：`RANK/WORLD_SIZE/LOCAL_RANK` 全部存在且 `WORLD_SIZE>1` 才进分布式，否则回退
  单进程单卡。多卡请用 `torchrun --nproc_per_node=N train.py ...`。

### 2. 验证改为完整采样 + 分项姿态损失（`train.py::validate`）
- 原 `validate` 复用训练的单步 flow-matching loss（随机 t，带噪、不评估真实姿态）。
- 改为跑 `inference.num_steps`(默认 10) 步 Euler ODE 采样（`model.sample`），分别计算：
  - **平移**：预测/真值的平均欧氏距离（米）；
  - **旋转**：6D→矩阵后的平均测地角（弧度）；
  - **关节**：关节角平均 L1 误差（弧度）；
  并对左右手取平均；DDP 下用 (sum,count) all-reduce 跨卡聚合。
  返回 `{trans, rot, joint, score}`，`score = trans+rot+joint`（越小越好）。

### 3. 只保留最好的 K 个检查点（新增 `training.keep_best_k`，默认 4）
- 按 `score` 升序在磁盘上只保留 top-K，文件名 `best_e{epoch:04d}_s{score:.4f}.pt`；
  新检查点不进前 K 则不保存，进前 K 则保存并删掉最差的。
- resume 时 `scan_best_checkpoints` 从文件名重建 registry，跨重启正确维护 top-K。
- 另存 `last.pt`（最新权重，供续训）。移除了原按 `save_interval` 的周期性全量保存。
- `configs/default.yaml`：`val_interval` 3000→5（原按 epoch 用，60 epoch 下几乎不触发）；
  删除失效的 `save_interval`；新增 `keep_best_k: 4`。

### 4. 模型真正支持 xyz-only（`models/pointnet2.py::PointNet2Encoder`）
- 配置 `pc_in_channels: 3` 表示 xyz-only，但 `encode_condition` 恒 `pc_encoder(xyz, rgb)`
  传 6 通道，导致首层 Conv（期望 3）崩溃。
- 让编码器在 `feature_dim==0`(即 in_channels=3) 时**自动丢弃颜色**走 xyz-only；
  `in_channels=6` 仍用 xyz+rgb。`encode_condition` 无需改动，6ch 向后兼容。
- 验证：用真实配置(pc_in_channels=3) 训练 forward/backward 与采样验证均跑通。

---

## 2026-06-28（一）— 适配 LGBiDex 数据集 + 路径/环境适配

### 背景
原 `test` 项目的数据集按 4 维四元数解析抓取，且数据路径指向远端 `/mnt/afs/...`，
无法直接在本机用我们的 **LGBiDex 双手数据集** 启动训练。本次改动参照 `slai/dexvlm`
（主线 `LgbiDexDataset`）的输入处理，让 `test` 适配该数据集，**未改动模型架构**。

### 数据格式（LGBiDex）
- split：JSON 数组，每条一例抓取。
- 关键字段：`obj_id`、`obj_pose`(7D=平移+wxyz 四元数)、`obj_scale`(~0.1)、
  `guidance`(指令文本)、`cate_id`、`dex_grasp_left`/`dex_grasp_right`
  （**28D = `[trans(3), axis_angle(3), joints(22)]`**，旋转是 axis-angle 不是四元数）。
- 物体资源：`{mesh_root}/{obj_id}/pc_part/simplified_part_{N}.ply`（彩色点云，优先）、
  `{mesh_root}/{obj_id}/mesh/simplified.obj`（网格，回退/算 bbox）。

### 改动文件
- **新增 `data/lgbidex_io.py`**：从 dexvlm 移植的纯 numpy/torch 工具——axis-angle→6D、
  wxyz 四元数→矩阵、PLY/OBJ 网格读取、面积加权表面采样、物体位姿变换、bbox 中心化、
  点云颜色选择。使 `test` 自包含、不跨项目依赖。
- **重写 `data/dataset.py`**：`DexGraspDataset` 镜像 dexvlm 的 LGBiDex 处理——
  彩色 PLY 优先 / 网格回退、按 `obj_pose` 旋转+缩放放置、`center_on_object` bbox 中心化、
  双手 28D axis-angle → 31D 目标位姿 `[trans(3), rot6d(6), joints(22)]`；
  输出仍为模型契约 `xyz(N,3)/rgb(N,3)/text/pose_left(31)/pose_right(31)`，
  collate 产出 `gt_poses (B,2,31)`。按物体缓存原始几何（内存有界）。
  保留 `sample_point_cloud_from_mesh` 供 `inference.py`。
- **`configs/default.yaml`**：
  - 数据路径改为本机 lgbidex（`train_v3.json`/`test_v3.json`）+ 本机 mesh_root；
  - `n_points=4096`（须匹配 PLY 的 `simplified_part_{N}.ply`）；
  - 新增数据参数 `point_cloud_source/point_cloud_color_mode/point_cloud_color_fill/`
    `center_on_object/obj_pose_quaternion_order`；
  - `augment=false`（原随机 Z 旋转只转点云不转抓取，会破坏点云/抓取对应关系，已禁用）；
  - **修正 `model.pc_in_channels: 3 → 6`**：模型 `encode_condition` 始终传 rgb，
    3 通道会让 PointNet++ 首层 Conv 崩溃。这是配置值修正，**未动模型代码**。
- **`train.py` / `test.py` / `inference.py`**：传递新数据参数；默认 `n_points=4096`。
- **`CLAUDE.md`**：更新数据约定文档。

### 验证（本机真实数据，CPU）
- 数据集：xyz/rgb `(4096,3)`、pose `(31,)`、rot6d 正交误差 1.2e-7 / det=1.0、
  点云物体中心化、真实 PLY 颜色、batch `gt_poses (4,2,31)`。
- 完整链路：模型构建(105M) → forward(loss≈1.385) → backward(梯度正常) →
  eval 采样输出双手 translation/rotation_6d/joints。

### 启动训练
```bash
/mnt/conda/jiaxuan/miniconda3/envs/dexvlg/bin/python train.py --config configs/default.yaml
```
（离线时模型自动回退随机初始化 BERT；需预训练权重则联网或提供本地 BERT 路径。
 快速冒烟可把 `train_data` 临时指向 `Dataset/lgbidex/train_head1000.json`。）

### conda 环境（dexvlg）—— 同时适配 H100 与 5090
- 路径：`/mnt/conda/jiaxuan/miniconda3/envs/dexvlg`
  （注：`slai/miniconda` 是远端挂载、本机无执行权限，故用系统 conda）。
- **torch 2.11.0+cu128，`arch_list = [sm_75, sm_80, sm_86, sm_90, sm_100, sm_120]`**，
  即**同一套环境同时支持 H100（sm_90）与 RTX 5090（sm_120）**，目标机 H100 无需重装。
- 已装：torch/torchvision、transformers 5.12.1、pyyaml、tqdm、tensorboard、scipy、trimesh。
  open3d 仅 `inference` 可视化用，可选。
- H100 备注（可选优化，非必需）：H100 原生支持 bf16，可把 `training.fp16` 改为 bf16 autocast
  以获得更稳的数值；当前 fp16 在 H100/5090 上均可正常训练。
