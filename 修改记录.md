# 修改记录

> 记录格式：日期 / 问题编号 / 改动文件:行 / 摘要 / 验证。
> 问题编号 A–M 对应 2026-07-10 全面审查（5 维度多 agent 查找 + 每发现 2 视角对抗验证，
> 12 项确认 + 1 项存疑全部修复）。修复计划见
> `~/.claude/plans/home-jiaxuan-exp-pami-test-task-type-dg-snuggly-cray.md`。

---

## 2026-07-10 — 全面审查修复（P0 评测口径 / P1 休眠陷阱 / P2 卫生 / P3 下轮训练）

### P0 — 评测口径与可复现性

**A. 语言 pad token 泄漏 cross-attention，预测随 batch 组成漂移（medium）**
- 根因：`FusionTransformer` 只在自身 encoder 内屏蔽 pad key（`models/dexvlg.py`），全长 fused
  （含 pad 派生行）返回后，`FlowMatchingBlock.cross_attn`（`flow_matching.py:61`）与
  `ReasonerBlock.cross_attn`（`latent_reasoner.py:56`）无 key_padding_mask；tokenizer
  `padding=True` 使 pad 数随 batch 变化。实测同一样本换 batch 位姿差 3–6mm / 2–3°。
- **短期修（零重训，兼容旧 ckpt）**：`model.lang_padding` 配置（"longest"=旧行为默认 /
  "max_length"=定长 128）；`dexvlg.py:266-275,411`、SimpleTokenizer 同步（`:39-75`）；
  `infer_dataset.py` 加 `--lang-padding` CLI 覆盖。FPS 独立漂移源同修：
  `pointnet2.py:28-51,149-151` eval 模式 FPS 起点固定（训练保持随机=增广）。
- **根治（需重训）**：mask 全链透传，开关 `model.mask_pad_tokens`（默认 False=旧行为）。
  `encode_condition` 返回 `(fused, key_padding_mask)`（affordance token 补 False），4 个调用方
  （compute_loss/sample/compute_loss_latent_ar/sample_latent_ar）接双返回值；
  `FlowMatchingBlock/Transformer.forward` 加 `memory_key_padding_mask`（latent_ar 侧对
  cat([memory,z,h,prev]) 拼接非 memory 段全 False）；`ReasonerBlock/_forward_prefix/4 个
  rollout` 加 `mem_key_padding_mask`。`configs/v3_multitask.yaml`、`v3_smoke.yaml` 置
  `lang_padding: max_length` + `mask_pad_tokens: true`（**改此项需重训**）。
- 验证：flag off + longest 与改前参考张量 16 项组合 **逐位一致（diff=0）**；batch 不变性：
  旧配置漂移 8.5e-02 → max_length 1.2e-07 / mask on 2.4e-07；flag on 训练冒烟 loss 有限、
  backward 234 个梯度全有限；test_sanity 10/10。
- ⚠️ 可比性断点：eval 态 FPS 确定性 + 推理配置 max_length 使**新评测数字与
  dexvlg_v2e29_s5 及更早不逐位可比**（该批已用旧行为跑完落袋）。旧 ckpt 推理请用其训练
  快照 config（无新键→旧行为）或显式 `--lang-padding`；`mask_pad_tokens: true` 仅对
  重训后的新 ckpt 有意义。

**G. missing_hand 静默排除出 bench 分母（存疑项，设防）**
- `prepare_bench_data.py`：missing_hand 按通道分计（`:389,398`）；每通道写
  `<exp_dir>/convert_summary.json`（kept/saved/samples/missing_hand/task_type_skipped/
  intended_total，`:431-461`）；终端提示双口径（evaluated=succ/eval，
  intended=succ/(eval+missing_hand)，`:497-499`）。dgbench 侧 glob 仅 `*.npy`，sidecar 无干扰。
- 验证：GT 小样四通道 sidecar 数字自洽（kept+missing=intended）；人工删手用例 lgbidex 4+1=5、
  bidex 3+2=5 正确。本次 v2e29 全量运行 missing_hand=0（口径未受影响）。

**F. test.py latent_ar 评测默认截断且不留痕（low）**
- `test.py:252-260`：打印"评测 N/M 条（val_max_batches=K, batch=B）"+截断 WARNING；
  `:266-269` metrics 注入 `n_evaluated/n_total/truncated/val_max_batches`。全量需
  `--val-max-batches 0`。

**H. make_gt_pred 先截断后过滤 → GT 基线通道缺失（medium）**
- `tools/make_gt_pred.py`：改为遍历全量→先 task_type 过滤→`--per-type N` 每类配额
  （满额提前退出）；`--n` 变为过滤后总量上限；打印每类实际收取数。
- 验证：`--per-type 5` 四类各 5；`--per-type 5 --n 7` 总量封顶；filter+per-type 提前退出正确。

**B. matrix_to_quaternion 对角并列最大返回全零（low）**
- `utils/rotation.py:92-95`：trace<=0 分支改 argmax 选轴，消除全零路径。
  `test_sanity.py:29-52` 新增退化用例（[1,1,0]/√2、[1,1,1]/√3 的 180° round-trip）。
- 验证：2000 随机旋转 5.96e-07；500 随机轴 180° 全非零 5.36e-07。影响面仅 inference.py
  demo（bench 链用独立实现，未动）。

### P1 — 休眠陷阱

**C. EMA 开启时选型-部署脱钩（medium）**
- `test.py/infer_dataset.py/inference.py` 各加 `--use-ema`（按需加载 ema_state_dict）；ckpt 含
  EMA 而未指定时警告"验证分数来自 EMA 权重，当前加载 raw"。`train.py:913-917` ckpt 增
  `val_weights: "ema"|"raw"` 元信息。当前 ema.enabled=false，无既有 ckpt 受影响。

**D. checkpoint 非原子写（low）**
- `train.py:921-925`：`torch.save(path+".tmp")` → `os.replace`。best/last/epoch/final 四类
  调用共用此函数。冒烟验证无 .tmp 残留。

**E. 无 --resume 也继承旧 run best 注册表（low）**
- `train.py:1179-1195`：scan 仅 `--resume` 时执行；非 resume 且目录含 `best_*.pt` 打脏目录
  警告（列出旧文件与分数，提示可能被淘汰删除）。

### P2 — 配置与工具卫生

**L. 默认 config 指向失效 /mnt/afs（low）**
- 入口默认：test/inference/infer_dataset → `configs/v3_multitask.yaml`；train →
  `configs/v3_smoke.yaml`（防裸跑误启正式训练）。`configs/default.yaml` 移档
  `configs/legacy/default_v4_afs.yaml`。train.py/test.py 模块 docstring、
  `scripts/inference2.sh`（另修：删失效 `source /mnt/afs`；**dedup 默认键补 scale_id**——
  bidex 多尺度场景否则被折叠 ~1/3）、CLAUDE.md Common Commands 同步。

**K. norm stats 元信息矛盾（low）**
- `tools/compute_norm_stats.py:354` note 改由 split_path 生成；就地修
  `tools/norm_stats_v3.json`：source.note 如实（源=train_fuse_v1，已验证 v1→v2 姿态逐条
  相同，数值不过期），删失效 histograms 路径。**数值键逐字节未动**（normalizer 加载 +
  roundtrip 1.19e-07 验证）。

**M. compute_pose_stats.py 死工具（low）**
- 删除（v4 遗留、遇 fuse 单手记录即 KeyError、产物无人消费、被 compute_norm_stats.py 取代）；
  `data/pose_normalizer.py:45-46` 注释改指 compute_norm_stats.py。

**I. metric_eval 依赖 obj_id_old 丢 52% 数据（medium）**
- `convert_to_gays.py`：守卫改 obj_id+pred_key（obj_id_old 可选保留）；输出带 task_type；
  组键 (task_type, obj_id, action_id|tt_兜底, pose_id, scale_id) 防 bidex 塌组；skip 按
  task_type 分计打印。`eval_gays_q1.py/eval_gays_fid.py`：mesh 仍走 obj_id_old
  （OakInk coacd），新增 filter 显式打印每 task_type 覆盖/跳过（right=YCB、bidex=sem_* 无
  OakInk mesh，明示跳过）。`eval_dgtr_diversity.py` sample key 修 bidex 多尺度撞键。
  `run_all_metrics.sh`/`collect_summary.py` 加覆盖范围说明（diversity=四类全；
  Q1/FID=仅 left/lgbidex；后者 mesh 构建留作后续）。
- 验证：dexgys env 实跑 diversity 于新格式端到端成功（含 bidex）。

### P3 — 面向下一轮训练

**J. val==test 选型污染（low）+ val 划分**
- 新工具 `tools/split_val_from_train.py`（obj_id 排序 seed=42 抽 5% 物体）已执行：
  `train_fuse_v2_split.json`（1,131,368 条 / 3,060 物体）+ `val_fuse_v2_split.json`
  （49,624 条 / 161 物体，四 task_type 全覆盖）+ 物体清单 json。train+val=1,180,992 ✓、
  物体零交集 ✓（与 test 也零重叠）。
- `configs/v3_multitask.yaml` data 段改指 split（train/val），test 不变。**只影响之后启动的
  训练**；在跑的 `dexvlg_v2_scratch`（config 快照独立）不受影响，其 best 分数按旧口径
  （val==test）解读，偏乐观。

### 综合验证（全部批次合并后）
test_sanity 10/10；9 个入口/工具脚本 ast 解析通过；v3_multitask 新配置加载 + 三数据文件
存在 + normalizer 正常；GT 小样链路 make_gt_pred(--per-type) → 四通道转换 → sidecar 自洽。

### 同日更早（背景）
- 四通道下游评测落地：prepare_bench_data 四 task_type 分派 + bidex 掌心镜像夹紧 1cm；
  dgbench-pami 新建 eval4（双手同时抓，setting/lift_height 双场景）。GT 自检 left 60/60、
  right 49/60、lgbidex 60/60、bidex 桌面/浮空各 60/60。
- best_e0029 全量四通道评测（每场景 5 姿态，dedup 含 scale_id，403,605 次采样）：
  left 49.6%（25626/51660）、right 28.0%（57715/206205）、lgbidex 23.6%（14147/59920）、
  bidex 56.6%（48574/85820）；missing_hand=0。输出
  `/home/jiaxuan/slai/dgbench-pami/output/dexvlg_v2e29_s5_*`。
