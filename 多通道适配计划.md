# 多手抓取统一训练方案（单手 / 双手协同 / 双手大物体）

> 目标：把当前"纯双手桌面抓取"的 DexVLG 复现，扩展成**一个模型统一训练三类数据**：
> 1. **单手**（有左手样本，也有右手样本）；
> 2. **双手协同桌面抓取**（现状）；
> 3. **双手一起抓大物体**（两手间距大、平移 min-max 范围远大于现状）。
>
> 并按用户设想探索**自回归 + 思维链（CoT）**：不给模型显式 tag，让它自己逐步推导出
> "用几只手、用哪只手、什么尺度"的抓取方案。
>
> 本文＝现状诊断 + 相关调研 + 归一化规范方案 + 架构方案（3 选 1 + 推荐）+ 文件级改造清单 + 路线图。
> 现在**不需要**立刻改架构，这是"怎么做"的设计文档。

---

## 0. TL;DR（先看这个）

三件事要分开解决，别混在一起：

| 子问题 | 本质 | 推荐做法 |
|---|---|---|
| **单双手适配** | 变长输出（1 或 2 只手）+ 手别（左/右）由谁决定 | 把"手数/手别"变成**模型自己 emit 的决策**（手序列 + STOP），而不是输入 tag；训练时用 GT 监督 |
| **归一化范围差异** | 三类场景**只有"平移"分布差异大**，关节/旋转是手内在构型、基本不变 | 平移改成**物体尺度相对化 + 1/99 分位**；双手用**相对位姿**（第二只手相对第一只手）；关节保持 ROM 表，旋转直通 |
| **自回归 + CoT** | 用户想要"自己推导" | **别把每个标量都 token 化做长链自回归**（精度损失 + 误差累积）；用**短/隐式 CoT**（手别、尺度锚点、接触区域几个推理 token）驱动，末端**仍用 flow-matching 出连续位姿** |

**推荐架构 = 方案 C「Reason-then-Flow」**：主干产条件 → 自回归 emit 一小段"计划/推理 token"（隐式 CoT，含手数、手别、粗尺度锚点/接触区）→ **按手自回归**（第二只手条件在第一只手上）→ 每只手用现有 flow-matching head 出 31D 连续位姿。它同时解决三个子问题，且**保留了 flow-matching 的连续精度**，改动可控。

落地建议：**先做方案 A（低风险，2 天量级）跑通"混合数据 + 新归一化"**，验证数据/指标链路；再升级到方案 C 加"自回归 + CoT"。

---

## 1. 现状诊断（代码级：哪里挡住了三类场景）

当前 pipeline（已读全代码）：
`PointNet++（64 token×256d）→ Projector→768 → 与 BERT/ModernBERT 文本 token 做 4 层 FusionTransformer → FlowMatchingTransformer（DiT 风格，6 block）→ (B, 2, 31)`。

**硬编码"永远两只手"的地方**（这是最大障碍）：

- `models/dexvlg.py:301-309`：`FlowMatchingTransformer(num_queries=2)`，两只手 query 写死。
- `models/dexvlg.py:459`：采样 `x = torch.randn(B, 2, self.pose_dim)`；`:472` `for i, hand in enumerate(["left","right"])` —— 输出恒为左右两手。
- `data/dataset.py:345-346`：`_build_hand_pose(record,"left")` 和 `"right"` 都要读；单手记录没有另一只手就会崩。
- `data/dataset.py:368-370` `collate_fn`：恒 `torch.stack([pose_left, pose_right]) → gt_poses (B,2,31)`，每条样本**必须两只手都在**。
- `models/dexvlg.py:381-427` `compute_loss`：flow-matching MSE 对 (B,2,31) 全部一视同仁；单手样本会把"不存在的那只手"也算进损失，训出垃圾。
- `tools/compute_norm_stats.py:49`：统计脚本也是每条记录左右手各取一次 —— 全链路都假设"双手齐全"。

**归一化不适配多尺度**：

- `data/pose_normalizer.py:49-72`：`_LEFT_FACTOR_MINMAX` / `_RIGHT_FACTOR_MINMAX` 是**固定表**。其中：
  - 平移 3 行是**针对现状双手桌面分布手调**的（如 `left x [-0.28,0.27]`）；
  - 关节 22 行是 **Shadow Hand ROM 上下限**（与场景无关，正确，保留）；
  - 旋转 6D **直通**（测地意义不变，正确，保留）。
- 后果：**大物体双手**时两手间距大、真实平移会**超出 [-0.28,0.27]** → 归一化到 [-1,1] 外 → 饱和/截断 → 与 flow 的 N(0,1) 先验错配 → 大物体样本学不好；**单手**左/右分布又与双手不同，同一张表更不合适。

**关键：可用但没用起来的杠杆**：
- `obj_scale`（记录里有）+ mesh bbox（`_load_mesh_bbox_center` 已算）→ **物体尺度已知**，正是尺度自适应归一化的抓手。
- `models/flow_matching.py:117-127` 已经预留 **per-hand type embedding + cross-hand attention**（`hand_interaction`，现在关着）—— 双手协同建模现成可用。

---

## 2. 关键洞察：把问题拆对（这一节决定后面所有设计）

### 2.1 三类场景里，**只有"平移"是尺度敏感的**
31D = `[trans(3), rot6d(6), joints(22)]`。
- **joints(22)**：手指关节角，被手的运动学范围（ROM）限死，**跨场景基本同分布** → 保持固定 ROM 表即可，不用动。
- **rot6d(6)**：手腕朝向，是方向量、无尺度 → **直通**即可。
- **trans(3)**：手腕在物体坐标系里的位置，**唯一随物体大小/单双手/两手间距剧烈变化**的量。

> 结论：**"归一化范围差异"问题 ≈ "平移归一化"问题**。集中火力改平移，别动关节/旋转。这大幅简化了工程量。

### 2.2 单双手 = **变长输出**问题
不要用"两个 slot 里塞一个空手"的思路去凑（会训出"空手"这种病态目标）。正确抽象是：一次抓取要输出 **1 或 2（未来可 N）只手**，这是**变长集合生成**（DETR/自回归 + STOP 的经典范式）。因为这里序列极短（1–2），自回归的"误差累积"风险很小——反而是自回归最适合的场景。

### 2.3 "不用 tag，自己推导" = 把决策变成**模型 emit 的量**，而非输入
用户不想给 `mode=single/bimanual/large` 这种 tag。做法不是"没有这些信息"，而是**让模型自己把它们生成出来并被监督**：
- **用几只手** → 由"emit 手序列直到 STOP"隐式决定；
- **用哪只手** → 每只手 emit 一个"手别"决策（左/右），训练时 GT 监督；
- **什么尺度/抓哪里** → emit 一个粗"尺度/锚点/接触区"推理 token（DextER 式的 embodied 中间表示）。

这就是"思维链"：**先推出场景理解，再据此生成精确位姿**。它们是**输出**（被 loss 监督），不是**输入 tag**。

---

## 3. 相关工作调研（只挑对本任务有直接借鉴的）

**A. 直接同题的抓取生成**
- **DexVLG**（ICCV'25, arXiv:2507.02747，本项目复现对象）：VLM（Florence-2）+ flow-matching 位姿头，DexGraspNet 3.0（1.7 亿抓取/17.4 万物体），**单灵巧手、桌面、部件对齐**。→ 说明"VLM 条件 + flow-matching 出连续位姿"这条路本身是 SOTA，值得**保留 flow head**。
- **DextER**（CVPR'26, arXiv:2601.16046）：**与用户设想最贴近**。LLM 主干融合点云（PartField 部件感知编码器）+ 文本，**自回归先 emit "接触 token"（哪根手指链接触物体表面哪里），再 emit "抓取 token"（手构型）**。接触预测就是"embodied 思维链"——一个"具身感知的中间表示"，把语义桥接到物理约束，**不需要任务 tag**。→ 直接印证"自回归 + 接触/推理链 + 无 tag"可行；也提示**接触/接触区可作为 CoT 的载体**。
- **UltraDexGrasp‑20M**（arXiv:2603.05312）：**多策略**数据/模型——大物体双手托、中物体全手抓、小物体两指捏/三指抓。→ 正是"单手 + 双手 + 大物体"的多场景混合，证明**一个模型覆盖多策略**是成立方向。
- **BiDexGrasp**（arXiv:2604.06589）：**几何‑尺寸自适应**双手抓取，物体 30–80cm、970 万抓取。→ "尺寸自适应"正是归一化尺度问题的正解方向。
- **BimanGrasp**（arXiv:2411.15903）：首个大规模双手灵巧抓取合成数据 + 扩散模型。**DGTR**（arXiv:2404.18135）：DETR 式**集合预测**一次前向出多样抓取。**DexGYS/"Grasp as You Say"**（arXiv:2405.19291）、**OmniDexVLG**（arXiv:2512.03874，抓取分类学 + 功能 affordance）：语言引导灵巧抓取。

**B. VLA 的动作表示 / 自回归 vs flow**
- **π0**（arXiv:2410.24164）：**VLM 主干（PaliGemma）+ flow-matching "动作专家"**（MoE 式独立权重出连续动作）。→ **混合范式**：会推理的主干 + 连续 flow 头。**这正是方案 C 的蓝本**，且与本项目现有结构最兼容。
- **π0‑FAST / FAST**（arXiv:2501.09747）：**DCT + BPE** 把连续动作块压成离散 token 做自回归，比"逐维逐步分箱"更适合高频灵巧动作。→ 如果一定要走纯自回归离散（方案 B），**用 FAST 类频域 token，别用朴素逐维分箱**。
- **OpenVLA**（arXiv:2406.09246）动作离散化：每维 256 箱，**箱界取训练分布的 1/99 分位（不是 min/max）**以抗离群。→ **归一化规范**的关键：**用分位数而非 min/max**（正好本仓 `pose_normalizer.py` 顶部 v4 注释已经在算 1/99 分位平移了，是现成起点）。
- **Octo**（arXiv:2405.12213）：跨本体数据**重加权**平衡分布。→ 三类场景数据不均衡时**按场景重采样**。

**C. 思维链（CoT）在 VLA 里的经验与坑**
- **ECoT / Embodied CoT**（arXiv:2407.08693）：先自回归生成文本推理（拆子任务 + grounding 场景/机器人状态）再出动作。
- **CoT‑VLA**（arXiv:2503.22020）：视觉 CoT（预测子目标图像）。
- **DualCoT‑VLA**（arXiv:2603.22280）：**可学习的 CoT query token，单次前向做"压缩推理"**，避开自回归文本解码。
- ⚠️ **公认的坑**（多篇提到）：**把长 CoT 当作自回归动作前缀，会 compounding error（误差累积）、且推理慢**。→ **本任务的 CoT 要"短/隐式"**：几个推理 token 足矣，精确位姿交给 flow 头。这是方案 C 的核心取舍依据。

> 一句话总结调研：**"会推理的主干（可 emit 短推理链）+ flow-matching 连续位姿头"（π0/DextER 精神）**是当前最稳的折中；**接触/手别/尺度**是合适的推理链载体；**归一化用分位数 + 尺度相对化**是社区共识。

---

## 4. 归一化的**规范方案**（重点，用户特别强调"规范操作"）

### 4.1 组件分治（先定原则）
| 分量 | 是否尺度敏感 | 归一化策略 |
|---|---|---|
| joints(22) | 否（手内在 ROM） | **保持** Shadow ROM min-max（现状 `pose_normalizer.py` 关节行不变） |
| rot6d(6) | 否（方向量） | **直通**（现状即如此，只重正交化） |
| **trans(3)** | **是** | **重构**：尺度相对化 + 分位数标准化（下面 4.2–4.3） |

### 4.2 平移：**物体尺度相对化 + 1/99 分位**（解决"大物体范围更大"）
思路：手腕位置**相对物体尺寸**表达，让"抓在物体左面"无论对象是马克杯还是 60cm 箱子都映射到相近的归一化值，从而**把三类场景的平移分布收拢到同一区间**。

- 定义特征长度 `L_obj`＝物体 bbox 对角线长（或最长边）。点云已在 `_build_point_cloud` 里按 `obj_scale`/bbox 中心化，`_load_mesh_bbox_center` 已算 bbox → `L_obj` 几乎零成本可得。
- 相对平移：`trans_rel = trans / L_obj`（或 `/ (0.5*L_obj)`）。
- 对 `trans_rel` 再做**分位数标准化**：用**全体混合训练集**、**按手别**统计 1st/99th 分位，线性映到 [-1,1]（OpenVLA 式，抗离群）。
- 反归一化时乘回 `L_obj`（`L_obj` 每样本已知，随 batch 传入即可）。

> 为什么有效：大物体双手"间距大"主要是因为**物体大**；除以 `L_obj` 后，间距/位置变成"占物体多大比例"，与小物体可比。**尺度差异被物体尺寸吸收，剩余分布三类场景高度重叠**。这与 BiDexGrasp"尺寸自适应"同理。

### 4.3 双手：**相对/层级位姿**（进一步稳住分布，且天然配合自回归）
即使做了尺度相对化，**双手绝对位置**仍比**两手相对关系**更飘。做法：
- 选一个**锚点** anchor：双手取"两掌中点"（或物体中心），单手取该手自身。
- **第一只手**：预测其相对 anchor 的（尺度相对化后）平移；
- **第二只手**：预测其**相对第一只手**的平移增量 `Δ = trans2_rel − trans1_rel`。两手相对变换（间距/构型）在"桌面协同"和"大物体托举"里都远比绝对坐标稳定。
- 这与**方案 C 的"按手自回归"天然一致**：先出手 1，再"以手 1 为条件"出手 2 的相对量。

> 归一化与架构在这里**合流**：相对表示既压缩了分布，又是自回归的自然条件。

### 4.4 **规范的统计与校验流程**（"怎么算、怎么验"）
1. **扩展统计脚本**（改 `tools/compute_norm_stats.py`）：
   - 遍历**混合训练集**（三类都在）；对每条记录**只统计其实际存在的手**（单手记录只统计那一只）；
   - 在**选定表示**下统计（即 `trans_rel`，双手可再统计 `Δ`）；
   - 报告**每手别、每分量的 1st / 50th / 99th 分位 + mean/std**；
   - **分场景**各出一份直方图（单手/桌面双手/大物体双手）。
2. **产物**：写入 `tools/norm_stats_multi.json`（沿用现有 `meastd11`/`stats_path` 机制，新增 `mode: relquantile`）。
3. **校验（关键，别跳过）**：
   - 归一化后每分量应落在 ~[-1,1]，**截断比例 < 1–2%**（统计 |x|>1 的占比）；
   - **三类场景的归一化平移直方图应大面积重叠**（这是"归一化是否成功"的核心判据）；
   - 若大物体仍长尾 → 检查 `L_obj` 是否取对（用最长边或对角线试）、或对 `Δ` 单独放大分位区间。
4. **一致性**：训练在归一化空间，验证/推理**采样后立即反归一化回真实米/弧度**（现状 `train.py:validate`、`infer_dataset.py` 已有反归一化点，扩展 `L_obj`/相对项即可）。

### 4.5 备选（若不想引入相对表示）
- **纯全局分位**（只做 4.2 的分位、不除 `L_obj`）：最省事，但大物体尾巴会把桌面抓取挤到中间小带 → 常见场景精度下降。**不推荐单用**。
- **可学习"尺度 token"**（方案 C 里 emit 一个粗尺度码，模型据此选归一化模式）：把 4.2 的硬相对化换成"软"的、模型自选——更灵活但更重。**作为 C 的增强项**。

---

## 5. 单双手适配的**表示层**设计

### 5.1 变长：手序列 + STOP（推荐）或 slot + 掩码（保底）
- **推荐（配合自回归）**：一次抓取＝`[hand_1, hand_2, ..., STOP]`。模型逐手生成，emit STOP 结束 → 1 或 2 只手自然统一，未来加"三手/多物体"零成本。
- **保底（不改成自回归时）**：保留最多 2 个 query slot + **per-slot presence（存在位）**；单手 = 一个 slot 存在、另一个 mask 掉损失；模型额外预测 presence（BCE）。改动最小。

### 5.2 手别（左/右）由模型 emit，不做输入 tag
- 每只手输出里带一个**手别分类**（left/right），训练用 GT 监督（left/right 已在数据里）。
- 单手场景"该用左还是右"= 模型据几何 + 指令**推断**并 emit（这正是"自己推导"）。
- 归一化按 emit 的手别选对应分位表（左右各一套，现状已是左右分表）。

> 注意：现状左右手是"固定两 slot"，新设计里手别变成**内容**而非**位置**。collate/loss 要按"存在的手 + 手别"对齐，不能再假设 index0=左、index1=右。

---

## 6. 架构方案（三选一 + 推荐）

### 方案 A —— 低风险增量：Masked Multi-slot Flow（保留现架构）
- 保留 flow-matching；query slot=2 + **presence 头**；单手 mask 掉缺席手的 flow 损失。
- 手别用**可学习 role embedding**，模型预测 presence + role；不是输入 tag。
- 归一化上 §4.2（尺度相对化 + 分位）。
- **优点**：改动最小（~2 天）、复用现有 flow 头、快；能立刻验证"混合数据 + 新归一化"是否 work。
- **缺点**：不是真正的自回归/CoT；封顶 2 手；单手浪费一个 slot。
- **定位**：**第一步里程碑**，把数据/归一化/指标链路先跑通。

### 方案 B —— 用户设想：全自回归 + CoT（纯 VLA 风格）
- 把接触/手别/尺度/位姿**全部 token 化**（位姿建议 FAST 频域 token，别逐维分箱），序列：
  `[推理 token：场景/手数/尺度/接触区] → [hand_1 位姿 token] → [hand_2 位姿 token] → STOP`。
- **优点**：纯 CoT、最灵活、最贴合用户设想、可扩展 N 手/多物体。
- **缺点**：**大改**；离散化损失连续精度（抓取要 mm 级）；**长链 compounding error**；自回归采样慢；更吃数据量。
- **定位**：**长期目标**（若要做成真正的灵巧抓取 VLA 再上）。

### 方案 C —— **推荐：Reason-then-Flow（π0 / DextER 精神的混合）**
> 会推理的主干 emit **短/隐式 CoT** + **按手自回归** + **每手 flow-matching 连续位姿头**。

结构（在现有代码上演进）：
1. **主干条件**：PointNet++/（可换部件感知编码器）+ 语言 → FusionTransformer 出条件 token（现状保留）。
2. **推理链（隐式 CoT，自回归但极短）**：新增一小段 decoder，emit 若干**推理 token**：
   - 手序列控制：逐手 emit "继续/STOP" → 决定 1 或 2 手；
   - 每手**手别**（left/right）；
   - 可选**粗尺度锚点 / 接触区** token（DextER 式，落在点云 token 上的注意力/离散码）。
   这些 token **被 GT 监督**（手数/手别/接触可从数据算），但是**生成的**，不是输入 tag。可用 **DualCoT 式可学习 query token 单次前向**，避免长文本自回归的误差累积。
3. **按手自回归位姿**：`FlowMatchingTransformer` 从"固定 2 query"改为"**按手 query**"：先出手 1，再**以手 1 位姿 + 推理 token 为条件**出手 2（§4.3 的相对量）。`hand_interaction`/`cross_hand_attn`（现成，`flow_matching.py:117-127`）正好用来做两手协同。
4. **连续精度**：每手 31D 仍由 **flow-matching** 出（保留现有 head 和训练 loss），**不离散化位姿** → 保住 mm 级精度。
5. **归一化**：§4.2+§4.3 内建（手 2 相对手 1、平移尺度相对化）。

- **优点**：同时解决**变长 + 手别 + 尺度**；**保留 flow 连续精度**；CoT"短/隐式"→ 无长链误差累积；改动中等；与现有 flow/fusion/cross-hand 代码**高度复用**。
- **缺点**：比 A 多（新增推理 decoder + 按手自回归 + 相对解码 + masked/role/presence 损失）；需要设计推理 token 的监督信号。

### 对比速览
| 维度 | A 增量 | B 全自回归 | **C 推荐混合** |
|---|---|---|---|
| 变长单双手 | slot+mask（封顶2） | 序列+STOP（N手） | 按手自回归+STOP（N手） |
| 手别来源 | 预测 role | emit token | emit token |
| CoT/推理 | 无 | 长链（有误差累积风险） | **短/隐式（低风险）** |
| 位姿精度 | 连续(flow) | 离散(有损) | **连续(flow)** |
| 归一化多尺度 | §4.2 | §4.2 + 尺度token | **§4.2+§4.3** |
| 改动量 | 小 | 大 | 中 |
| 与现码复用 | 高 | 低 | **高** |
| 建议 | **先做** | 长期 | **主目标** |

---

## 7. 自回归 + 思维链的具体设计（方案 C 细化）

### 7.1 推理链 emit 什么、怎么监督
按"从粗到细、逐步推导"排布（每一步都可从数据造 GT，端到端监督）：
1. **手数/手序列**：逐手二分类"还有下一只手吗" → STOP。GT = 该记录实际手数（1 或 2）。
2. **手别**：每只手 left/right 分类。GT = 数据里的 left/right。
3. **（可选）尺度/锚点**：emit 粗锚点（掌心区域）或"接触区"——落在点云 token 上的注意力权重 / 或一个离散接触码（DextER 式）。GT 可用"手 mesh 与点云的接触点/最近点"离线算（弱标注即可）。
4. **位姿**：交给 flow head（§7.3）。

> 训练：推理 token 用交叉熵/BCE（离散决策）；接触用注意力监督或 BCE（若有接触 GT）。**多任务加权**，与 flow loss 联合。推理阶段"贪心/采样 emit"这几个 token，再喂给 flow head。因序列极短，误差累积可忽略。

### 7.2 为什么"短/隐式 CoT"而非"长 token CoT"
调研明确结论：**长 CoT 作自回归动作前缀会 compounding error + 慢**（ECoT 等）。本任务输出只有 1–2 手，把"用几手/哪只手/什么尺度"压成**几个决策 token**就够"推导"了；**精确位姿是连续量、最怕离散化**，交给 flow 最合适。DualCoT 的"可学习 query token 单次前向"进一步把 CoT 变成**并行隐式推理**，兼顾"会推理"与"不脆弱"。

### 7.3 按手自回归 + flow 位姿头（落到现有代码）
- `FlowMatchingTransformer.forward` 现在一次出 `num_queries=2`。改为**逐手**：
  - step 1：query=手1，条件=推理 token（+主干条件），flow 采样出手1的 31D；
  - step 2：query=手2，条件里**追加手1的位姿 embedding**（相对解码 §4.3），flow 采样出手2；
  - STOP 由 §7.1 的手序列控制决定是否进入 step 2。
- `hand_interaction.cross_hand_attn`（`flow_matching.py:121-123`，现成）用于两手 query 交流；单手时退化为单 query。
- 每手内部**仍是整块 flow-matching**（不逐维自回归），保精度。

---

## 8. 数据与训练链路改造清单（文件级 TODO）

> 说明：以下是"要动哪里、动成什么"，不是让你现在就改。**方案 A 只需 ①②③④ 的最小子集**；方案 C 再加 ⑤⑥。

**① 数据 schema（约定层）**
- 允许记录**只含 `dex_grasp_left` 或只含 `dex_grasp_right`**（单手），或两者都含（双手）。
- 每条带上（或可推出）：实际存在的手集合、手别；`obj_scale` 已有 → 由 bbox 出 `L_obj`。
- 建议加一个**仅用于分层采样/评测**的 `scenario` 字段（single/bimanual_table/bimanual_large）——**不作为模型输入 tag**，只用于数据均衡与分场景指标。
- ⚠️ **数据依赖（需你确认）**：三类数据是否**都已就绪**且同为 LGBiDex schema？还是大物体/单手需要**新合成**（可参考 UltraDexGrasp/BiDexGrasp 的多策略生成）？这决定工作量分配。

**② `data/pose_normalizer.py`**
- 新增平移归一化模式 `relquantile`：`trans_rel = trans / L_obj` 后按 1/99 分位标准化；关节 ROM、旋转直通不变。
- `LgbidexPoseNormalizer.normalize_pose/denormalize_pose` 接收每样本 `L_obj`（或在 dataset 里先算好相对量再进 normalizer）。
- 双手相对项 `Δ` 的（反）归一化。

**③ `tools/compute_norm_stats.py`**
- 见 §4.4：混合集、只统计存在的手、在相对表示下出 1/50/99 分位 + 分场景直方图 → `tools/norm_stats_multi.json`。

**④ `data/dataset.py`**
- `_build_hand_pose`：单手记录不再强取两只手；缺席手不产目标。
- `__getitem__`：产出**变长手列表 + presence + 手别 + `L_obj`**（而非固定 pose_left/pose_right）。
- `collate_fn`：pad 到该 batch 最大手数，带 **presence mask + 手别 + L_obj**；`gt_poses` 变 `(B, max_hands, 31)` + mask。
- 分组键 `group_id_of` 视情况纳入 scenario。

**⑤ `models/dexvlg.py` / `models/flow_matching.py`（方案 C）**
- `num_queries` 从固定 2 → **按手动态**；`sample()` 去掉 `randn(B,2,...)` 与 `["left","right"]` 硬编码，改成"按 emit 的手序列逐手采样"。
- 新增**推理 decoder**（手序列/手别/接触）+ 相应输出头。
- flow head 支持"以上一只手为条件"的相对解码；启用 `cross_hand_attn`。

**⑥ `train.py`**
- **masked flow loss**：只对存在的手算；presence(BCE) + 手别(CE) + 接触(可选) 多任务加权。
- **分场景验证指标**：单手成功率 / 桌面双手协同 / 大物体双手 分别报；双手额外报"两手相对位姿误差"。
- **均衡采样**：三类不均衡时按 scenario 重加权（Octo 式）或课程学习（§9）。`GroupedEpochSampler` 可扩展 scenario 维度。
- 变长下的组内最小匹配（`build_group_candidates`/`validate`）要按"手数一致 + 手别对齐"匹配。

**⑦ 推理/评测**（`infer_dataset.py` / `prepare_bench_data.py`）
- 输出变长手数；反归一化含 `L_obj`/相对项；bench 侧按单/双手分别评。

---

## 9. 分阶段落地路线图

- **P0 数据与归一化打底（先做，风险最低）**
  1. 确认/准备三类数据（§8①的依赖问题）。
  2. 实现 `relquantile` 归一化 + 扩展统计脚本，**跑 §4.4 校验**（三类直方图重叠、截断<2%）。**这一步单独就能验证"归一化范围差异"是否解决**。
- **P1 方案 A（masked multi-slot）跑通混合训练**
  - slot+presence+mask + 新归一化，混合三类数据训练；建立分场景指标基线。**验证数据/损失/指标链路正确**。
- **P2 方案 C（Reason-then-Flow）**
  - 加按手自回归 + 手别/手序列 emit + 相对解码 + 启用 cross-hand attn；接触 CoT 作为可选增强。
  - 对比 A 的分场景指标，确认 CoT/相对表示带来的增益。
- **P3（可选）方案 B / 强化**
  - 若要做成真正的灵巧抓取 VLA：换部件感知编码器（PartField 类）、FAST 频域动作 token、更长的接触推理链。

**课程/均衡建议**：单手（简单）→ 桌面双手 → 大物体双手 由易到难；或直接混合但**按 scenario 重采样**保证大物体/单手不被淹没。

---

## 10. 风险、取舍与验证清单

- **离散 vs 连续精度**：位姿离散化会丢 mm 级精度 → 方案 C 坚持位姿走 flow（只把决策/推理离散）。
- **CoT 误差累积**：长链自回归会放大误差 → 保持 CoT 短/隐式（§7.2）。
- **数据不均衡**：三类量级差异大 → 重加权/课程（§9）；否则模型偏向多数类。
- **归一化必须校验**：不看直方图就上训 = 重蹈"大物体饱和"覆辙。§4.4 的重叠/截断检查是硬门槛。
- **checkpoint 语义变化**：改归一化/表示后旧权重失效需重训（本仓 `修改记录.md` 已有此教训）。
- **左右不对称 & 双手协同**：启用 `hand_interaction`/`cross_hand_attn`；相对表示帮助协同。
- **评测口径**：单手成功率与双手"协同成功"是不同指标，别用一个 score 混评。
- **精度/AMP**：延续 `precision: bf16`（`修改记录.md` 里 fp16→bf16 的 NaN 教训）。

**"归一化是否做对"的验收标准（可直接拿去测）**：
1. 三类场景归一化后平移直方图**大面积重叠**；
2. 每分量 |x|>1 占比 **< 2%**；
3. 反归一化往返误差 ≈ 0；
4. 混合训练后**大物体双手**的平移误差与桌面双手**同量级**（不再被饱和拖垮）。

---

## 11. 参考文献（arXiv 编号）

- DexVLG（本项目复现对象）: 2507.02747 (ICCV'25)
- DextER（自回归 + 具身推理，最贴合用户设想）: 2601.16046 (CVPR'26)
- UltraDexGrasp‑20M（多策略：单手/双手/大物体）: 2603.05312
- BiDexGrasp（几何‑尺寸自适应双手）: 2604.06589
- BimanGrasp（双手灵巧抓取数据 + 扩散）: 2411.15903
- DGTR（集合预测多样抓取）: 2404.18135；DexGYS/"Grasp as You Say": 2405.19291；OmniDexVLG: 2512.03874
- π0（VLM + flow-matching 动作专家）: 2410.24164；FAST/π0‑FAST（频域动作 token）: 2501.09747
- OpenVLA（256 箱 / 1‑99 分位离散化）: 2406.09246；Octo（跨本体重加权）: 2405.12213
- ECoT（具身思维链）: 2407.08693；CoT‑VLA（视觉 CoT）: 2503.22020；DualCoT‑VLA（隐式 CoT query token）: 2603.22280

> 注：部分为 2026 年新作，方法细节以各自论文正文为准；本文用于指导本项目扩展的**设计取舍**，具体超参/实现以复现为准。